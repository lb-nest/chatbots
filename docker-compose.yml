services:
  db:
    image: postgres
    environment:
      - POSTGRES_DB=db
      - POSTGRES_PASSWORD=pwd
  server:
    build: .
    ports:
      - 3030:8080
    env_file:
      - .env
    depends_on:
      - db
